# 系统修复计划

## [ ] 任务1：修复教师工号和姓名显示
- **优先级**：P1
- **依赖**：无
- **描述**：
  - 教师工号应该显示t008等自定义工号，而不是随机ID
  - 姓名应该显示正常的中国姓名
  - 调整教师列表的显示逻辑
- **成功标准**：
  - 教师列表中工号显示为t001、t008等格式
  - 姓名显示为正常的中国姓名
- **测试要求**：
  - `programmatic` TR-1.1：教师列表页面加载时，工号和姓名显示正确
  - `human-judgement` TR-1.2：教师列表显示美观，信息清晰
- **注意**：需要检查教师数据的存储和读取逻辑

## [ ] 任务2：词表清单添加AI生成标记
- **优先级**：P1
- **依赖**：无
- **描述**：
  - 在词表清单中显示是否有AI生成的配套练习和例句
  - 添加视觉标记便于教师和管理员判断
- **成功标准**：
  - 词表卡片上显示AI生成标记
  - 标记清晰可辨
- **测试要求**：
  - `programmatic` TR-2.1：词表卡片加载时显示AI标记
  - `human-judgement` TR-2.2：标记位置合理，不影响其他信息显示
- **注意**：需要检查词表数据结构，确保AI生成信息被正确存储

## [/] 任务3：修复学生学习记录和积分同步
- **优先级**：P0
- **依赖**：无
- **描述**：
  - 学生学习记录和积分不能同步到数据库
  - 学生端显示积分，但教师端显示为0
  - 退出后重新登录积分清空
- **成功标准**：
  - 学生积分同步到数据库
  - 教师端能看到学生的积分
  - 退出后重新登录积分保留
- **测试要求**：
  - `programmatic` TR-3.1：学生学习后积分增加并同步到数据库
  - `programmatic` TR-3.2：教师端能查询到学生的积分
  - `programmatic` TR-3.3：退出后重新登录积分不变
- **注意**：需要检查dataSync模块和API调用

## [ ] 任务4：修复学生端词表显示重复
- **优先级**：P1
- **依赖**：无
- **描述**：
  - 学生端选择单词闯关时，词表信息显示两遍
  - 只需要显示一遍词表清单
- **成功标准**：
  - 词表信息只显示一遍
  - 点击词表能正确跳转到游戏页面
- **测试要求**：
  - `programmatic` TR-4.1：词表弹窗只显示一遍词表信息
  - `human-judgement` TR-4.2：词表显示美观，无重复
- **注意**：需要检查学生端的词表弹窗渲染逻辑

## [ ] 任务5：修复Zootopia游戏页面错误
- **优先级**：P1
- **依赖**：任务4
- **描述**：
  - 词表没有关联到游戏页面
  - 浏览器后端有错误
  - ReactDOM.render过时警告
- **成功标准**：
  - 词表正确关联到游戏页面
  - 浏览器后端错误解决
  - ReactDOM.render警告修复
- **测试要求**：
  - `programmatic` TR-5.1：游戏页面能加载选择的词表
  - `programmatic` TR-5.2：浏览器控制台无错误
  - `programmatic` TR-5.3：ReactDOM.render警告消除
- **注意**：需要检查zootopia.html中的数据加载和React渲染逻辑

## 实现顺序

1. 任务3：修复学生学习记录和积分同步（最高优先级）
2. 任务4：修复学生端词表显示重复
3. 任务5：修复Zootopia游戏页面错误
4. 任务1：修复教师工号和姓名显示
5. 任务2：词表清单添加AI生成标记

## 测试流程

每个任务完成后，需要进行以下测试：
1. 功能测试：验证修复是否生效
2. 兼容性测试：确保不影响其他功能
3. 性能测试：确保页面加载和操作流畅

## 预期完成时间

所有任务预计在2小时内完成。