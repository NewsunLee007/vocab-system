# 2026-03-29

## Vercel 500 错误修复

- 问题：部署到 Vercel 后 `/api/auth/login`、`/api/auth/me`、`/api/school/data` 均返回 500
- 根因1：`prisma/schema.prisma` 缺少 `directUrl` 配置，Neon + Vercel serverless 必须同时设置连接池 URL 和直连 URL
- 根因2：DATABASE_URL 和 DIRECT_URL 末尾有 `&channel_binding=require`，Prisma 不支持此参数
- 修复：
  1. `prisma/schema.prisma` 添加 `directUrl = env("DIRECT_URL")`
  2. `.env` 去掉两个 URL 的 `&channel_binding=require`
  3. 已 commit（f07e2bd），待用户手动 push
- Vercel 环境变量已配置，登录功能恢复正常 ✅
- 另外修复：vercel.json 添加 outputDirectory:"." 解决构建报 No Output Directory 错误
- 修复：db.js _data 初始化缺少 dict:{} 字段，导致 getStats() 报 TypeError: Cannot convert undefined to object
- 修复：admin 词表导入 xlsx/csv 功能 — 之前用的是假的 simulateExcelParse，现改用 SheetJS 真实解析；readAsBinaryString 改为 readAsArrayBuffer；CSV UTF-8 编码；input accept 补上 .csv

## 教师账户管理体系修复（2026-03-29 上午）

- 问题：教师登录报 404，在 admin 教务处新建教师后仍无法登录
- 根因：
  1. `vercel.json` 有 `/api/(.*)` → `/api/$1` 的多余 rewrite 规则，干扰 Vercel 原生 API 路由，导致 404
  2. 教务处 `admin.js` 的 `saveNewTeacher()` 只调用 `db.addTeacher()`（写入 SchoolData JSON），不写 User 表；而教师登录 `api/auth/login` 查的是 User 表，两边数据完全割裂
- 修复：
  1. `vercel.json` 删除冗余 API rewrite 规则，改为仅对非 API 路由重写到 index.html
  2. 新建 `api/admin/teachers.js`：支持 GET/POST/DELETE，由管理员操作真实 User 表
  3. `js/core/api.js` 添加 fetchTeacherAccounts / createTeacherAccount / deleteTeacherAccount 方法
  4. `js/modules/admin.js` 全面改造：renderTeacherList/saveNewTeacher/deleteTeacher/confirmBatchDelete/confirmBatchImport 全部改为异步 API 调用
  5. 教师列表表头更新（工号/账户状态/创建日期/操作）
- commit: 8badc8c，已 push ✅

## 教务处弹框点击无效 bug 修复（2026-03-29 上午）

- 问题：已教师登录状态下，点教务处登录弹框的"登录"按钮无响应，控制台显示实际调用的是 `loginTeacherFromModal` 而非 `loginAdminFromModal`，且 POST /api/auth/login 返回 401
- 根因：`showAdminLogin()` 没有先隐藏教师登录弹框，两个 `fixed z-[100]` 弹框叠在一起，教师弹框在前（DOM 顺序靠前），拦截了所有点击事件
- 修复：
  1. `app.js showAdminLogin()`：打开教务处弹框前强制隐藏教师弹框，并清空密码框
  2. `auth.js performAdminLogin()`：登录前先 `clearToken()` + 清除 `currentUser`，避免旧 token 残留
  3. `auth.js performTeacherLogin()`：同样在登录前清除旧 session
- commit: e2e5ec8，已 push ✅

## 已登录后仍弹出登录框 bug 修复（2026-03-29 上午）

- 问题：教务处已登录并刷新页面后，再点导航栏"教务处"按钮，会再次弹出登录弹框，但页面已在 admin 视图
- 根因：`updateNav()` 登录后从未隐藏 `nav-login-links`（教师端/教务处按钮），用户能继续点按钮触发弹框
- 修复：
  1. `updateNav()`：登录后隐藏 `nav-login-links`，登出后恢复显示
  2. `showAdminLogin()`：已以 admin 身份登录时直接跳转 admin 视图，不弹框
  3. `showTeacherLogin()`：已以 teacher 身份登录时直接跳转 teacher 视图，不弹框
  4. 顺手清理 `hideAdminLogin()` 中的调试 console.log
- commit: 5febf92，已 push ✅


- 问题：导入词表弹框中选择 xlsx 文件后，"确认导入"按钮仍为禁用状态，无法点击
- 根因：`index.html` 的 `excel-file-input` 没有绑定 `onchange` 事件，导致 `handleExcelFileSelect()` 从未被调用，`disabled` 永远不会解除
- 修复：
  1. `index.html`：给 input 加上 `onchange="admin.handleExcelFileSelect(event)"`
  2. `admin.js showExcelPreview()`：补上显示 `import-preview` 预览区域的逻辑
  3. `admin.js showImportWordlistModal()`：重置时同时隐藏预览区域
- commit: 9a11b39，已 push ✅

## 确认导入词表 TypeError 修复（2026-03-29 上午）

- 问题：点击"确认导入"后报 `TypeError: Cannot read properties of null (reading 'value')` at admin.js:1701
- 根因：`index.html` 列映射区域只有 col-word 和 col-meaning 两个 select，而 JS 还读 col-textbook/col-grade/col-volume/col-unit/col-phonetic/col-pos 共6个不存在的元素
- 修复：
  1. `index.html`：补全8个列映射下拉框（教材/年级/册别/单元/单词/释义/音标/词性）
  2. `admin.js confirmImportWordlist()`：改用 `?.value ?? ''` 安全读取防崩溃
- commit: 9db4463，已 push ✅

## AI 处理词表 CORS 错误修复（2026-03-29 下午）

- 问题：点击"AI处理词表"后报 CORS 错误，前端直接调用 DashScope API 被浏览器拦截
- 错误：`Access to fetch at 'https://dashscope.aliyuncs.com/...' from origin '...' has been blocked by CORS policy`
- 根因1：`aiSentenceService.js callAIBatch()` 和 `teacher.js callExternalAI()` 直接从浏览器 fetch 外部 AI API，浏览器 CORS 策略不允许
- 根因2：`AI_PROVIDERS.qwen.defaultEndpoint` 配置的是原生 DashScope 格式 URL，不是 OpenAI 兼容格式，发出的请求体格式也不对
- 修复：
  1. 新建 `api/ai/proxy.js`：Vercel Serverless Function，接收前端请求并转发到真实 AI API（服务端不受 CORS 限制），支持 OpenAI 格式和 Anthropic 格式
  2. `aiSentenceService.js callAIBatch()`：改为 POST `/api/ai/proxy`，body 携带 endpoint/apiKey/messages 等参数
  3. `teacher.js callExternalAI()`：同样改为请求代理
  4. `admin.js AI_PROVIDERS.qwen`：endpoint 改为 OpenAI 兼容格式 `https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions`，模型列表更新
- commit: b6466a4，已 push ✅

## AI 素材无法固化到数据库问题修复（2026-03-29 上午）

- 问题：AI 生成的例句和检测素材在刷新页面后全部丢失，无法持久化
- 根因：
  1. `db.syncToCloud()` 上传的 `schoolData` 只包含 teachers/students/wordlists/tasks，没有 `aiDrafts`
  2. `db.init()` 从云端恢复时也没有还原 `aiDrafts`
  3. `startAIGeneration()` 生成完成后只保存在内存（`this._aiAnalysisResult`），没有调用 `db.saveAIDraft()`
- 修复：
  1. `db.js init()`：`_data` 初始化加 `aiDrafts: {}`，从云端恢复时读取 `d.aiDrafts || {}`
  2. `db.js syncToCloud()`：上传时在 `schoolData` 中加入 `aiDrafts: this._data.aiDrafts || {}`
  3. `teacher.js startAIGeneration()`：AI 生成成功后立即调用 `db.saveAIDraft(wordlist.id, {...}, currentUser.id)`
- 注：网络错误 `ERR_NAME_NOT_RESOLVED/ERR_NETWORK_CHANGED` 是本地网络问题，上线 Vercel 后代理在服务端执行不受影响；AI 失败时已有 `generateFallback` 降级到内置引擎
- commit: c79b1f9，已 push ✅

## 学生登录 404 修复 + AI 配置云端化 + 教务处 AI 生成（2026-03-29）

### 1. 学生登录 404 修复
- 问题：教师端添加学生后，学生登录报 `POST /api/auth/login 404`
- 根因：`saveNewStudents()` 和 `saveImportStudents()` 只调用 `db.addStudent()` 写 SchoolData，没有在 PostgreSQL User 表创建账号；登录接口查 User 表找不到返回 404
- 修复：
  1. `teacher.js saveNewStudents()`：改为 async，逐个调用 `api.register()` 在数据库创建用户（默认密码=姓名，首次登录强制修改），注册成功后再 `db.addStudent()` 用返回的真实 id
  2. `teacher.js saveImportStudents()`：同样改为 async，forEach 改为 async for 循环

### 2. AI 配置云端化（加密存储）
- 需求：教务处配置 API Key 后，教师端自动获取，不需要每人单独配置
- 方案：
  1. `db.js` 新增 `_encryptKey()` / `_decryptKey()`（Base64+反转混淆）
  2. `db.saveAIConfig()`：存储时将 apiKey 加密成 apiKeyEnc，明文只保留在本地 session
  3. `db.getAIConfig()`：优先读云端的 aiDrafts 下的 aiConfig，自动解密 apiKeyEnc
  4. `db.syncToCloud()`：同步时加入 `aiConfig`（不含明文 apiKey，只含 apiKeyEnc）
  5. `db.init()`：从云端恢复时加载 aiConfig，同步到 system.aiConfig

### 3. 教务处词表管理 AI 生成功能
- 需求：教务处词表管理页面，每行加 AI 生成按钮
- 修复：
  1. `admin.js renderWordlists()`：每行增加"AI生成"按钮，检查是否有 aiDraft 显示已生成/未生成状态
  2. `admin.js triggerAIGenerate(wordlistId)`：新方法，检查 AI 配置 → 检查已有草稿（提示覆盖）→ 调用 aiSentenceService.generateMaterials() → 保存到 db.saveAIDraft()

- commit: a321aee，已 push ✅（2026-03-29 10:42）

## 修复教务处 AI 生成参数错误（2026-03-29 10:52）
- 问题：admin.js 的 `triggerAIGenerate()` 调用 `aiSentenceService.generateMaterials(wordlist, aiConfig)` 参数错误
- 根因：`generateMaterials` 签名是 `(words, grade, onProgress)`，传入了整个 wordlist 对象和 aiConfig 对象
- 修复：改为 `generateMaterials(wordlist.words, grade, onProgress)` 并加进度回调更新 loading 文字
- commit: 375350c，已 push ✅

## 批量重置密码+学生端修改密码+教务处全权限+修复崩溃（2026-03-29 14:43）

### 1. 修复 viewStudentDetail 崩溃
- 问题：teacher.js 478 行尝试访问 student-detail-name 等不存在的静态 ID
- 修复：改为动态生成 student-detail-content 的 innerHTML，自带密码修改记录展示

### 2. 修复 editStudent 崩溃
- 删掉对不存在的 edit-student-id input 的赋值

### 3. 批量重置密码
- 批量操作 bar 加「批量重置密码」按钮
- teacher.confirmBatchResetPassword() 方法

### 4. 学生端修改密码（不强制）
- 头部加「修改密码」按钮，modal-student-change-password 模态框
- api/auth/change-password.js：验证旧密码后更新
- 修改记录存入 student.passwordChangeLogs，教师端详情可查

### 5. 教务处拥有教师端全部权限
- router.js：admin 允许访问 teacher 视图
- 教务处头部加「教师工作台」按钮
- teacher 视图头部加「返回教务处」按钮（admin 进入时显示）
- db.js：getStudentsByTeacher/getWordListsByTeacher/getLearningLogsByTeacher/getTasksByTeacher 当 teacherId==='admin' 时返回全量数据

### 其他
- git remote 切到 SSH 以解决 HTTPS 443 超时问题
- commit: 5c86ae3，已 push ✅

## 学生默认密码修复 + 教师端重置密码 + 教务处AI生成复用（2026-03-29 14:12）

### 1. 学生默认密码改为 123456
- teacher.js 两处 api.register 改为 `password: '123456'`

### 2. 教师端新增重置密码按钮
- 新建 api/auth/reset-password.js（TEACHER/ADMIN 权限）
- api.js 新增 resetStudentPassword() 方法
- 学生卡片加 🔑 重置密码按钮，teacher.resetStudentPwd() 方法

### 3. 教务处 AI 生成改为直接调用教师端完整流程
- admin.js triggerAIGenerate 简化为调 teacher.triggerAI(wordlistId)
- commit: a15f1da，已 push ✅

## 合并 Serverless Functions 解决 Vercel Hobby 12个限制（2026-03-29 17:52）

- 问题：Vercel Hobby 计划限制最多 12 个 Serverless Functions，项目有 13 个（auth 目录 6 个各自独立）
- 方案：将 auth 目录下 6 个文件（login/logout/me/register/reset-password/change-password）合并为单个 api/auth/index.js，通过 URL 路径最后一段分发
- 同时更新 vercel.json 添加 `/api/auth/:action` → `/api/auth/index` 的 rewrite 规则
- 前端 api.js 的 URL 路径完全不变，无需修改
- 合并后总函数数：13 → 8，余量充裕
- commit: 95e1c66，已 push ✅

## 首次登录修改密码 400 错误修复（2026-03-29 18:14）

- 问题1：学生/教师首次登录进入强制修改密码页，点确认后 POST /api/auth/change-password 返回 400 Bad Request
- 根因：api.js changePassword() 发送字段名是 `oldPassword`，但服务端 handleChangePassword 校验的是 `currentPassword`，字段名不一致导致服务端认为参数缺失
- 修复：api.js 中 changePassword(oldPassword, newPassword) 改为 changePassword(currentPassword, newPassword)，body 改为 { currentPassword, newPassword }

- 问题2：admin.js 报 SyntaxError: Unexpected token '}' at admin.js:2379
- 根因：triggerAIGenerate 方法末尾有两个 }（多一个），导致整个 admin 对象语法错误
- 修复：删除多余的 }

- commit: 248ad20，已 push ✅

## 页面加载就弹出首次修改密码框 bug 修复（2026-03-29 18:18）

- 问题：打开网页就直接出现"首次登录 - 修改密码"弹框
- 根因：
  1. auth.init() 从 api.me() 恢复旧 session，发现 passwordChanged=false，设置 currentUser
  2. app.init() 调 router.navigate(role)，router.checkPermission 检测到 passwordChanged=false，调用 auth.showForceChangePasswordModal()
  3. 此时没有 oldPassword（不在登录流程里），弹出空的改密码框
- 修复：
  1. auth.init()：恢复 session 时若 passwordChanged=false，清除 token、不设置 currentUser，返回 false → 用户被路由到 login 页重新登录
  2. router.checkPermission()：passwordChanged=false 时直接 return false（不再弹框），交由路由回 login 处理
- commit: 3f26c84，已 push ✅

## 学生学习中心数据同步修复（2026-03-29 19:50）

- 问题：学生学习中心功能几乎全部失效，统计数据不显示
- 根因：
  1. 学生登录后 db.init() 在 auth 完成后才执行，导致 student 数据未加载
  2. student.render() 读取 user.coins/streak 等字段，但这些数据存在 db.getStudentStats() 里
  3. 数据未正确规范化，导致显示异常
- 修复：
  1. auth.js 添加 buildStudentSession() 方法，从 db.getStudentStats() 获取完整统计
  2. auth.js 添加 refreshCurrentUserProfile() 在 db.init() 后刷新用户数据
  3. app.js init() 中 db.init() 后调用 refreshCurrentUserProfile()
  4. db.js 规范化 students/wordLists/dict 数据
  5. db.js 同步 difficultWords/learningHistory/teacherReviewedSentences 到云端
  6. student.js 修复检测模式选择逻辑（移除拼写必选要求）
- commit: cc24132，已 push ✅

## 学生导航栏背景色修复（2026-03-29 19:55）

- 问题：学生学习中心导航栏变成白色背景，文字看不清
- 根因：导航栏使用 `bg-white/10` 半透明背景，在某些情况下会被 backdrop-blur 效果放大白色
- 修复：根据用户角色动态设置导航栏背景色
  - 学生：翡翠绿渐变 `from-emerald-900 to-teal-900`
  - 教师：深蓝渐变 `from-indigo-900 to-slate-900`
  - 管理员：深灰渐变 `from-slate-900 to-slate-800`
- commit: a5cab15，已 push ✅

## 学生学习中心布局重构（2026-03-29 20:00）

- 问题：学生主界面布局不合理，任务区域太大，统计数据被挤成竖条
- 根因：HTML 结构错误（头部信息缺少 `</div>`），导致布局混乱
- 新布局方案：
  1. 顶部：标题 + 统计数据横向排列（更紧凑）
  2. 中间：两个学习模式卡片（自主学习 + 单词闯关）
  3. 下方：我的任务列表
- 删除底部多余的 3 列统计区域（已合并到顶部）
- 统计数据加图标：📚 已学单词、🔥 连续学习、💰 获得金币
- commit: 09bae57，已 push ✅

## 导航栏透明度 + 学习记录同步修复（2026-03-29 20:15）

### 1. 导航栏透明度
- 问题：导航栏是不透明的深色，需要更透明
- 修复：将所有角色的导航栏背景改为 70% 透明度
  - 学生：`bg-emerald-900/70`
  - 教师：`bg-indigo-900/70`
  - 管理员：`bg-slate-900/70`

### 2. 学习记录同步问题
- 问题：学生的学习记录没有同步到后端数据库，教师端看不到
- 根因：`db.syncToCloud()` 只在 `admin/teacher` 角色时同步 `schoolData`，学生身份时跳过
- 修复：让所有角色都能同步 `schoolData`，包括 `learningLogs`

### 3. teacherId 丢失问题
- 问题：学生数据同步后 `teacherId` 字段丢失，导致学习记录无法按教师过滤
- 根因：`normalizeStudent()` 使用 `...student` 展开，但没有显式保留 `teacherId`
- 修复：`normalizeStudent()` 显式添加 `teacherId: student.teacherId || null`

- commit: 73ecfa7，已 push ✅

## 学生同步数据权限不足修复（2026-03-29 20:30）

- 问题：学生端同步数据时提示"没有权限同步"
- 根因：`api/school/data.js` 的 POST 请求只允许 ADMIN 和 TEACHER 角色，STUDENT 被拒绝
- 修复：修改权限检查，允许 STUDENT 角色也能 POST 同步学校数据到云端
  - 学生需要同步学习记录、积分等数据到后端
  - 修改权限判断：`user.role !== 'ADMIN' && user.role !== 'TEACHER' && user.role !== 'STUDENT'`
- commit: 56aac07，已 push ✅

## 未登录时加载数据 401 错误提示修复（2026-03-29 20:30）

- 问题：刷新页面时弹出"云端数据加载失败：No token provided"错误提示
- 根因：`db.init()` 在用户未登录时调用 `api.fetchSchoolData()` 返回 401，原代码对所有错误都弹出提示
- 修复：判断如果是 401 错误或"No token"错误，静默失败不弹出提示
- commit: 056a5ef，已 push ✅

## 语音播放 interrupted 错误静默处理（2026-03-29 20:35）

- 问题：连续点击语音播放按钮时，控制台一直输出"语音播放失败: interrupted"错误
- 根因：
  1. `speechSynthesis.cancel()` 中断当前播放会触发 `interrupted` 错误事件
  2. 原代码将所有错误都视为失败并重试
  3. 重试逻辑用 `setTimeout` 导致 Promise 链断裂
- 修复：
  1. 识别错误类型：`interrupted` 和 `canceled` 是正常中断，静默处理
  2. 修复重试逻辑：改为 async/await 确保正确返回
  3. 正确获取错误类型：`error.error` 或 `error.message`
- commit: edb6519，已 push ✅

## AI 生成选择题正确答案随机分布修复（2026-03-29 20:45）

- 问题：AI 生成的选择题正确答案都在 A 选项，没有随机分布
- 根因：
  1. `aiSentenceService.js` 使用 `indexOf(word)` 查找正确答案位置
  2. 大小写不一致或找不到时会返回 -1
  3. 没有保护措施处理异常情况
- 修复：
  1. 使用 `findIndex` 配合 `toLowerCase()` 大小写不敏感查找
  2. 找不到时强制设为第一个选项并输出警告日志
  3. 对单词选项和释义选项都做了修复
- commit: 0d92574，已 push ✅

## 退出登录时全屏视图清理修复（2026-03-29 20:55）

- 问题：学生端在测试页面退出登录后，显示"已安全退出"但测试界面还在，没有返回登录页
- 根因：
  1. 测试页面使用 `fixed inset-0` 全屏覆盖
  2. `auth.logout()` 调用 `router.navigate('login')` 跳转到登录页
  3. 但全屏测试视图没有被清理，继续遮挡在登录页面上方
- 修复：
  1. `auth.logout()` 时移除所有测试/学习视图元素
  2. 包括：matching-test-view、flashcard-learning-view、spelling-mode、quiz-mode 等
  3. 同时取消所有正在播放的语音
- commit: 204ca04，已 push ✅

## 学生详情页文字颜色优化（2026-03-29 21:00）

- 问题：学生详情页的标签和表头文字颜色太浅，看不清楚
- 根因：使用 `text-slate-400` 在深色背景上对比度太低
- 修复：
  1. 统计卡片标签文字：`text-slate-400` → `text-slate-200`
  2. 学习记录表头文字：`text-slate-400` → `text-slate-200`
- commit: c3031d9，已 push ✅

## 学生详情模态框深色主题修复（2026-03-29 21:05）

- 问题：学生详情模态框配色不对，文字看不清
- 根因：
  1. 模态框使用 `glass-card`（浅色背景）
  2. 内部内容使用 `bg-slate-700/50`（深色背景）
  3. 标题 `text-slate-800`（深色文字）在深色背景上对比度低
- 修复：
  1. 模态框改为 `bg-slate-800 border border-white/10`（深色主题）
  2. 标题改为 `text-white`
  3. 关闭按钮改为 `text-slate-400 hover:text-white`
- commit: ea2098e，已 push ✅

## AI 提示词要求正确答案随机分布（2026-03-29 21:55）

- 问题：AI 生成的题目正确答案总是在 A 选项
- 根因：
  1. 提示词示例中 `correctIndex: 0` 表示正确答案在第一个位置
  2. AI 会按照示例模式生成所有题目
  3. 没有明确要求随机分布正确答案
- 修复：
  1. 添加要求 5：正确答案必须随机分布在 A/B/C/D 四个选项中
  2. 添加要求 6：说明 correctIndex 含义（0=A, 1=B, 2=C, 3=D）
  3. 修改示例：正确答案放在第二个位置（correctIndex: 1）
- commit: 487665d，已 push ✅

## 强制打乱 AI 选项顺序确保随机分布（2026-03-29 22:10）

- 问题：AI 返回的选项分布不够随机，集中在 B，A 和 D 较少
- 根因：
  1. 依赖 AI 的随机性不可靠
  2. 即使提示词要求随机，AI 仍倾向于某些位置
- 修复：
  1. 在 `generateWithExternalAI` 中接收 AI 数据后强制打乱
  2. 使用 Fisher-Yates 算法打乱 context 和 matching 选项
  3. 打乱后重新计算 correctIndex
  4. 确保正确答案真正均匀分布在 A/B/C/D
- commit: 035c9a8，已 push ✅

## 拼写模式语音播放功能增强（2026-03-29 22:30）

- 问题：学生听单词拼写单词练习没有发音
- 可能原因：
  1. 浏览器自动播放策略阻止未经用户交互的音频
  2. 语音播放失败未捕获错误
  3. 语音队列未清理
- 修复：
  1. 添加 `.catch()` 捕获播放失败
  2. 浏览器阻止时提示用户点击页面
  3. 播放前先 `cancel()` 取消之前的播放
  4. 添加 `onerror` 处理语音合成错误
  5. 添加 console.log 便于调试
- commit: 0b941f6，已 push ✅
